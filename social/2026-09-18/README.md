# Prepared price update

Status: prepared only. Do not publish or schedule until the live Kit checkout and website both show $9.99.

This native SVG/PNG updates the existing free-log-versus-binder comparison to the approved $9.99 price. The full caption, alt text and exact destinations are in `campaign.json`. Reuse the existing scheduled post if it is still pending; inspect the live queue before updating. Do not duplicate a published post.

The graphic is 1080 × 1350. It contains no customer or account information, product mockup, or paid product PDF. It was adapted from the existing Home Routine Guide vector design; no generative image tool was used for this social graphic.

## Website product image

The website product cover was edited with the built-in image-generation tool to change the badge to "$9.99" and add "Illustrative mockup • Digital download only • No physical binder shipped", preserving the existing brand, square layout and remaining wording. Project files: `../../new-homeowner-starter-binder.png` and `../../new-homeowner-starter-binder.webp`. The PNG retains the original generated output; the WebP is an optimized format conversion.
